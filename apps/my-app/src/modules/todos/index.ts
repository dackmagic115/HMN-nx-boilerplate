import Book from '@material-ui/icons/Book'
import { TodoList } from './ToDoList'
import { ShowGuesser } from '../../components'

export const Todos = {
    name: 'todos',
    list: TodoList,
    icon: Book,
    show: ShowGuesser
}